{
  "version": 2,
  "framework": "nextjs",
  "buildCommand": "npm run lint && npm run build",
  "devCommand": "npm run dev",
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  },
  "ignoreCommand": "git diff --quiet HEAD^ HEAD ./"
}